import 'package:flutter/material.dart';
import 'package:music_playaer/constants/colors.dart';

textStyle({double? size = 14.0, Color color = white, FontWeight? weight}) {
  return TextStyle(
    fontSize: size,
    fontWeight: weight,
    color: white,
  );
}
